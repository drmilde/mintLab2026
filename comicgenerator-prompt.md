I need a gradio app, that allows me to generate a script for a comic book page with 4 to 6 panels. I want to use this app as an easy to use interface for a mint course for girls at the age of 12. The course is in German. The interface should allow the children to describe up three characters that are shown on the page. there should be an input field, that allows to describe the overall setting of the scene. I also want to specify the description of the start scene and the final target or goal of the scene. Reference to the characters are by using their names. Based on this information, a script like description of the panels (between 4 and 6) should be generated using a call to a running ollama server with a gemma4 model running. for each panel there must be a description of the image of the panel, the text of the text box describing the panel as part of the story, and a possible dialog between the characters in the panel. this final script should then be displayed in a larger textarea, to be inspected and possibly changed by the user. Please create the code in such a way, that the general behavior of the system as part of a hidden context, that is added to the prompt to ollama is configurable as an .env file. use the apprpoiate python libraries for this.